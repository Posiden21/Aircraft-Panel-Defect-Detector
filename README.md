# Aircraft-Panel-Defect-Detector
ROS 2 computer vision package that detects aircraft-style panel surface defects with OpenCV, publishes annotated images, emits JSON detection metadata, and logs detections to CSV for later dataset work. It shows ROS 2 nodes, topics, launch files, parameters, OpenCV perception, synthetic data generation, and a basic dataset logging workflow.
